pipeline {
	agent any
	stages {
		stage ('Checkout Code') {
			steps {
				echo 'Code checkout' 
				script {
					def exists = fileExists 'TestAutomationProject'
					if (!exists){
						new File('TestAutomationProject').mkdir()
					}
					dir ('TestAutomationProject') {
						git url: 'http://localhost:3000/basil/MercuryTours.git'
						bat 'mvn -f TestAutomationProject\\pom.xml clean'
					}
				}
			}
		}

		stage ('Sanity Tests') {
			parallel {
				stage('Firefox') {
					steps {
						echo '****** FIREFOX ******'
						script {
							dir("TestAutomationProject") {
								try {
									bat 'mvn -f TestAutomationProject\\pom.xml test -Dcucumber.options="--tags @smoke01" -Dcucumber.options=\"--plugin json:target/cucumber-firefox.json'
									cucumber fileIncludePattern: '**/cucumber-*.json', parallelTesting: true
								} catch (err) {} 
							}
						}
					}
				}

				stage('Chrome') {
					steps {
						echo '****** CHROME ******'
						script {
							dir("TestAutomationProject") {
								try {								
									bat 'mvn -f TestAutomationProject\\pom.xml test -Dcucumber.options="--tags @smoke02" -Dcucumber.options=\"--plugin json:target/cucumber-chrome.json'
									cucumber fileIncludePattern: '**/cucumber-*.json', parallelTesting: true
								} catch (err) {} 
							}
						}
					}
				}
			}
		}		
	}
}